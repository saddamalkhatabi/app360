<!-- Generated from data/catalog.json by tooling/build-blueprints.js; edit the catalog, then regenerate. -->

# رفيق المرافق: خطط جلسة لعب مستجيبة

- الفئة: 1-4
- الهوية الثابتة: `a1-parent-play`
- المسار الوحيد: `apps/1-4/responsive-play-coach/`
- الحالة: مخطط للبناء؛ لا يوجد تطبيق تشغيلي بعد
- أولوية البناء داخل الفئة: 3

## الحاجة والناتج

أداة للكبير يجهز بها جلسة قصيرة: يلاحظ اهتمام الطفل ويختار استجابة لغوية ويخطط نهاية هادئة ثم يراجع تجربته.

**الناتج:** خطة جلسة لعب وسجل ملاحظة واحد بعد تنفيذها.

**المستخدم:** المرافق البالغ؛ الطفل يشارك في اللعب خارج الشاشة.

## نطاق النسخة الأولى وتسلسل الشاشات

- اختيار اهتمام ومادة لعب
- كتابة استجابة متوقعة وخطة انتقال
- مراجعة ما فعله الطفل وتعديل الجلسة القادمة

## الملاءمة والتدرج

في الطرف الأصغر من الفئة تكون الشاشة للمرافق واللعب واقعيًا؛ عند الاستعداد يتدرج الطفل من إشارة وتقليد إلى محاولة ذاتية. لا حروف إلزامية ولا جلسة مستقلة مطولة.

ترتبط المراجعة بالناتج المحدد أدناه. يمكن طباعة الخطة وتنفيذها بعيدًا عن الشاشة؛ في الإجابات المفتوحة يستخدم معيار وصفي ومراجعة المستخدم أو المدرب، بلا درجة آلية تدعي قياس المهارة كاملة.

## الارتباطات المحددة بالأهداف

الارتباط مساهمة محددة في الهدف، لا ادعاء تغطية كل أجزائه. الخطط ليست تطبيقات عاملة ولا دليل أثر مثبت. مهام المدرب مستقلة ومبينة في كل ارتباط.

### المدرب: اللعب الغني باللغة والتفاعل المستجيب

المفتاح: `coach-1-4-language-rich-play-responsive-interaction`

المساهمة والنشاط: التدرب على الانتظار ووصف اهتمام الطفل بدل اختباره يطبق اللعب المستجيب

دليل الملاحظة: يكتب المرافق استجابة الطفل وتوسيعه لها

الحالة: مطلوب بناؤه ضمن النسخة الأولى.

### المدرب: التنظيم المشترك والانتقالات ودعم نوبات الغضب

المفتاح: `coach-1-4-co-regulation-transitions-and-tantrum-support`

المساهمة والنشاط: إعداد نهاية متوقعة للعب وخيار مساعدة يدرب الانتقال والتنظيم المشترك

دليل الملاحظة: مراجعة انتقال واحد وما يحتاج تعديله

الحالة: مطلوب بناؤه ضمن النسخة الأولى.

## بيانات النسخة الأولى

`session_id` · `task_id` · `attempts` · `artifact` · `reflection` · `assistance_level`

يحفظ الناتج المناسب لهذا التطبيق ومحاولاته محليًا بمفتاح يتضمن الهوية ونسخة schema. حدّد شكل artifact من الناتج المذكور؛ لا تجمع بيانات غير لازمة. عند رفض التخزين يستمر النشاط مع تنبيه واضح وخيار تصدير، ولا يعد المستخدم بحفظ غير حاصل. التسجيل اختياري مع بديل نصي أو ملاحظة مرافق.

## التقنية وحدود البناء

Runtime: `static-web` · Depth: `standard` · Offline المستهدف: `offline-first`.

القدرات المستهدفة عند التنفيذ: `storage.local`، `pwa.offline`.

النسخة الأولى محلية بمحتوى أصلي أو مرخص؛ لا حسابات أو إرسال تلقائي أو توسع خارج المهمة الموصوفة.

اقرأ FRAMEWORK_AR وARCHITECTURE_AR وDEPENDENCY_POLICY_AR وPWA_OFFLINE_POLICY_AR في docs. لا تستورد من تطبيق آخر ولا تنسخ أصول الرسم. أضف القدرات الصوتية أو التسجيلية إلى العقد إذا بنيت بالفعل؛ لا تفترض أن سجل القدرات مكتبات جاهزة.

هذا المجلد حجز رسمي للخطة فقط. عند بدء التنفيذ استخدم `node tooling/scaffold-app.js 1-4 responsive-play-coach "رفيق المرافق: خطط جلسة لعب مستجيبة"`؛ يحتفظ بالمواصفات والهوية. يتولد عندها package.json وواجهة وManifest وأيقونات وSW، ثم ابنِ الممارسة؛ القالب وحده لا يجعل الحالة live. ثبّت dependencies من الجذر وحدّث lockfile الواحد.

واجهة عربية RTL مع تكبير الخط ودعم لوحة المفاتيح واللمس وأزرار بديلة للسحب. أمثلة الإنجليزية مجموعة مستقلة عند وجود محتوى لغوي؛ لا تخلط تعليم قواعد اللغتين. اختبر الهاتف والتابلت والحاسوب، والوضع الأفقي والرأسي. التوافق القديم مطلوب للرسم الحالي؛ لا تدّع دعم جهاز قديم في تطبيق جديد دون اختبار.

## قبول الممارسة

- يمكن طباعة الخطة وتنفيذها بعيدًا عن الشاشة
- لكل سجل ملاحظة فعلية وخطوة تحسين

- يتحقق الناتج: خطة جلسة لعب وسجل ملاحظة واحد بعد تنفيذها.
- كل ارتباط مدرب له نشاط واضح؛ لا تستبدله بعرض عنوان الهدف.
- بعد البناء تعمل حلقة كاملة وحفظ وإعادة فتح وتصدير أو حذف حسب نوع الناتج.
- اختبر الانقطاع ورفض التخزين أو الميكروفون حيث ينطبق، والتثبيت والتحديث دون فقد الناتج.
- قبل live نفذ مراجعة ملاءمة وتجربة مستخدم موثقة، وحدّث الحالة والرابط والعقد وشغّل validator.

## أساس الفكرة والمراجع

تصميم مقترح للمختبر يستأنس بالنمط الموثق في المراجع؛ ليس نسخًا للتطبيق المرجعي أو ادعاءً بفعالية نسختنا قبل اختبارها.

- [Harvard — Serve and Return](https://developingchild.harvard.edu/key-concept/serve-and-return/)
