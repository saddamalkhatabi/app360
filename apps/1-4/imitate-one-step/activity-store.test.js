'use strict';const test=require('node:test');const assert=require('node:assert/strict');const api=require('./activity-store.js');function storage(){const x={};return{setItem(k,v){x[k]=String(v)},getItem(k){return Object.prototype.hasOwnProperty.call(x,k)?x[k]:null},removeItem(k){delete x[k]}}}test('completion count increments and note is stored',()=>{const s=api.create(storage(),'t');let r=s.markDone('001','فكرة أفضل','2026-09-18T00:00:00Z');assert.equal(r.count,1);assert.equal(r.notes[0].text,'فكرة أفضل');r=s.markDone('001','', '2026-09-18T01:00:00Z');assert.equal(r.count,2);assert.equal(r.notes.length,1);assert.equal(s.total(),2)});test('notes can be saved without increment',()=>{const s=api.create(storage(),'t2');const r=s.addNote('061','استخدم كوبًا أخف');assert.equal(r.count,0);assert.equal(r.notes.length,1)});test('falls back to memory when storage denied',()=>{const bad={setItem(){throw Error('x')},getItem(){throw Error('x')},removeItem(){throw Error('x')}};const s=api.create(bad,'t3');s.markDone('141','');assert.equal(s.get('141').count,1);assert.equal(s.isPersistent(),false)});