(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root)root.ExperienceEngine=api;}(typeof self!=='undefined'?self:this,function(){'use strict';function norm(v){return String(v||'').toLowerCase().replace(/[أإآ]/g,'ا').replace(/ة/g,'ه').replace(/ى/g,'ي').trim()}function filter(items,q){q=q||{};var text=norm(q.text),levels=q.levels||[],families=q.families||[],availability=q.availability||[],energy=q.energy||'all',place=q.place||'all';return(items||[]).filter(function(x){if(levels.length&&levels.indexOf(x.level)<0)return false;if(families.length&&families.indexOf(x.family)<0)return false;if(availability.length&&availability.indexOf(x.availability)<0)return false;if(energy!=='all'&&x.energy!==energy)return false;if(place!=='all'&&x.place!==place&&x.place!=='any')return false;if(text){var hay=norm([x.title].concat(x.tags||[],(x.tools||[]).map(function(t){return t.name})).join(' '));if(hay.indexOf(text)<0)return false}return true})}function pick(items,seed){if(!items||!items.length)return null;var n=typeof seed==='number'?Math.abs(seed):Date.now();return items[n%items.length]}function map(rows){var out={};(rows||[]).forEach(function(x){out[x.id]=x});return out}return{normalize:norm,filter:filter,pick:pick,familyMap:function(meta){return map(meta.families)},levelMap:function(meta){return map(meta.levels)}};}));