'use strict';
const fs=require('node:fs');
const test=require('node:test');
const assert=require('node:assert/strict');
const games=require('./games-data.js').concat(require('./games-data-v12.js'));
test('library has exactly 170 creative games',()=>{assert.equal(games.length,170);assert.equal(new Set(games.map(g=>g.id)).size,170);});
test('four readiness levels remain balanced after the 20 checkmark exercises',()=>{const expected={1:43,2:43,3:42,4:42};for(let level=1;level<=4;level++)assert.equal(games.filter(g=>g.level===level).length,expected[level]);});
test('G71 through G90 are multi-choice mark exercises',()=>{for(let n=71;n<=90;n++){const id='G'+n;const g=games.find(x=>x.id===id);assert.ok(g,id+' missing');assert.equal(g.type,'mark');assert.ok(Array.isArray(g.markOptions)&&g.markOptions.length>=12,id+' needs large option set');assert.ok(g.markOptions.filter(x=>x[1]).length>=3);assert.ok(g.markOptions.filter(x=>!x[1]).length>=3);assert.equal(g.frames.length,5);}});
test('all 170 games use supported direct play types and paper materials',()=>{const supported=new Set(['choice','match','sort','dots','path','pattern','memory','draw','order','puzzle','create','mark']);for(const g of games){assert.ok(supported.has(g.type),g.id+' unsupported');assert.ok(Array.isArray(g.materials)&&g.materials.length>=1);assert.equal(g.frames.length,5);}});
test('mark engine supports large selections, target toggling and printable checkboxes',()=>{const js=fs.readFileSync('./app.js','utf8');const css=fs.readFileSync('./styles.css','utf8')+fs.readFileSync('./v8.css','utf8');assert.match(js,/function buildMark\(box,g\)/);assert.match(js,/function markWorksheetSVG\(g\)/);assert.match(js,/g\.type==='mark'/);assert.match(js,/تحقق من الاختيارات/);assert.match(css,/\.mark-option/);});
test('five-step overview is the default and single-step view expands only on demand',()=>{const js=fs.readFileSync('./app.js','utf8');assert.match(js,/function setupReel\(\)/);assert.match(js,/showOverview\(\)\}/);assert.match(js,/reel-stage is-overview/);assert.match(js,/reel-stage is-single/);assert.doesNotMatch(js,/if\(compactReel\(\)\)\{show\(0\);play\(\)\}else showOverview\(\)/);});
test('shared name audio remains centralized',()=>{const js=fs.readFileSync('./app.js','utf8');const html=fs.readFileSync('./index.html','utf8');assert.match(js,/function speakCheer\(\)/);assert.match(html,/Audio360Base/);assert.match(html,/drawing-writing-foundations\/audio-v21\.js/);});

test('dots and paths share one geometry between viewer canvas and printable lesson',()=>{const js=fs.readFileSync('./app.js','utf8');assert.match(js,/function tracePoints\(g\)/);assert.match(js,/function tracePoint\(pt,w,h\)/);assert.match(js,/function traceSvg\(g,i\)/);assert.match(js,/return traceSvg\(g,i\)/);assert.match(js,/paintGuide\(\)/);assert.match(js,/ابدأ من الدائرة الحمراء رقم 1/);});
test('all trace games contain usable ordered coordinates',()=>{for(const g of games.filter(x=>x.type==='dots'||x.type==='path')){const pts=g.type==='dots'?g.points:g.path;assert.ok(Array.isArray(pts)&&pts.length>=3,g.id+' missing trace points');for(const p of pts){assert.ok(Array.isArray(p)&&p.length===2);assert.ok(p[0]>=0&&p[0]<=100&&p[1]>=0&&p[1]<=100,g.id+' point out of range');}}});
test('v6 overview removes forced tall thumbnails and adds tablet 3 by 2 clarity grid',()=>{const css=fs.readFileSync('./styles.css','utf8');assert.match(css,/v6: synchronized dots and paths/);assert.match(css,/grid-template-columns:repeat\(3,minmax\(0,1fr\)\)/);assert.match(css,/height:auto!important/);});

test('v7 checkboxes stay visually subordinate to the child choice',()=>{const css=fs.readFileSync('./styles.css','utf8');const js=fs.readFileSync('./app.js','utf8');assert.match(css,/child-friendly subordinate checkboxes/);assert.match(css,/width:22px;height:22px/);assert.match(css,/font-size:40px/);assert.match(js,/انظر إلى الصورة أولًا/);assert.match(js,/width=\"24\" height=\"24\"/);});

test('v8 opens games directly and exposes previous next game navigation',()=>{const js=fs.readFileSync('./app.js','utf8');assert.match(js,/function navigateGame\(delta\)/);assert.match(js,/id="prevGame"/);assert.match(js,/id="nextGame"/);assert.match(js,/launchWorksheetDesigner\(g\)/);});
test('v8 reel overview has dynamic height and single-step expansion',()=>{const js=fs.readFileSync('./app.js','utf8');const css=fs.readFileSync('./v8.css','utf8');assert.match(js,/reel-stage is-overview/);assert.match(js,/reel-stage is-single/);assert.match(css,/height:auto!important/);});
test('v8 checkmark indicator is subordinate and below the visual target',()=>{const css=fs.readFileSync('./v8.css','utf8');assert.match(css,/\.mark-box\{position:absolute/);assert.match(css,/bottom:7px/);assert.match(css,/width:18px/);});
test('v8 provides A4 worksheet design tools and reuses stable say-and-name assets',()=>{const js=fs.readFileSync('./app.js','utf8');assert.match(js,/function openWorksheetDesigner\(game\)/);assert.match(js,/1\.4142/);assert.match(js,/say-and-name\/assets\/objects/);assert.match(js,/corrected-elephant-v24\.jpg/);});
test('shared ui icon library exists for legacy-safe navigation',()=>{for(const f of ['games.svg','print.svg','draw.svg','prev.svg','next.svg','check.svg'])assert.ok(fs.existsSync('../../../assets/ui-icons/'+f),f+' missing');});

test('v8 uses a Flexbox fallback for old tablets instead of requiring CSS Grid',()=>{const css=fs.readFileSync('./v8.css','utf8');assert.match(css,/v8 legacy layout fallback/);assert.match(css,/display:flex!important/);assert.match(css,/flex-wrap:wrap!important/);});
test('v8 service worker precaches shared legacy-safe icons and object art',()=>{const sw=fs.readFileSync('./sw.js','utf8');assert.match(sw,/assets\/ui-icons\/games\.svg/);assert.match(sw,/say-and-name\/assets\/objects\/cat\.svg/);});

test('dynamic profile and finish buttons keep stable SVG icons',()=>{const js=fs.readFileSync('./app.js','utf8');assert.match(js,/updateUserButton\(\).*uiIcon\('user'/s);assert.match(js,/finish\.innerHTML=uiIcon\('finish'/);});

test('v9 reverses previous next arrows for Arabic RTL navigation',()=>{const html=fs.readFileSync('./index.html','utf8');const js=fs.readFileSync('./app.js','utf8');assert.match(html,/globalPrevBtn[^\n]+ui-icons\/next\.svg/);assert.match(html,/globalNextBtn[^\n]+ui-icons\/prev\.svg/);assert.match(js,/uiIcon\('next','اللعبة السابقة'\)/);assert.match(js,/uiIcon\('prev','اللعبة التالية'\)/);});
test('v9 gives every one of the 90 games its own cover image',()=>{const games=require('./games-data.js');for(const g of games)assert.ok(fs.existsSync(`./assets/game-covers/${g.id}.svg`),g.id);const js=fs.readFileSync('./app.js','utf8');assert.match(js,/assets\/game-covers\/.*g\.id/);});
test('v9 worksheet designer exposes search upload drag resize templates and persistence',()=>{const js=fs.readFileSync('./designer-v9.js','utf8');for(const x of ['Wikimedia Commons','wdUpload','ondragstart','ondrop','data-resize','wdSize','wdAutoSize','wdRows','wdCols','data-ready','saveLocal','exportJson','importJsonFile'])assert.match(js,new RegExp(x));});
test('v9 designer stays ES5 friendly for Android 4.4 class browsers',()=>{const js=fs.readFileSync('./designer-v9.js','utf8');assert.doesNotMatch(js,/=>|\bconst\b|\blet\b|`/);assert.match(js,/ontouchstart/);assert.match(js,/App360CloseDrawing/);assert.match(js,/aspectRatio.*clientWidth/s);});
test('v9 designer asset catalog is large and includes local assets and game covers',()=>{const src=fs.readFileSync('./designer-assets-v9.js','utf8');assert.match(src,/ScreenMoveDesignerAssets/);assert.match(src,/say-and-name\/assets/);assert.match(src,/game-covers\/G90\.svg/);assert.ok(src.length>5000);});
test('designer service worker preserves designer shell and game cover offline assets',()=>{const sw=fs.readFileSync('./sw.js','utf8');assert.match(sw,/screen-to-move-v1[012345]/);assert.match(sw,/designer-v9\.js\?v=1[012345]/);assert.match(sw,/game-covers\/G01\.svg/);assert.match(sw,/game-covers\/G90\.svg/);});

test('worksheet settings use a user-controlled edge drawer',()=>{const js=fs.readFileSync('./designer-v10.js','utf8');const css=fs.readFileSync('./v10.css','utf8');assert.match(js,/wdSettingsToggle/);assert.match(js,/toggle\.onclick/);assert.match(css,/\.wd-settings-drawer\{[^}]*display:none/s);assert.match(css,/\.wd-settings-drawer\.open\{display:block/);});
test('v10 designer buttons keep their labels visually prominent',()=>{const html=fs.readFileSync('./index.html','utf8');const css=fs.readFileSync('./v10.css','utf8');assert.match(html,/designer-feature-btn[^>]*>.*مصمم أوراق الألعاب/s);assert.match(html,/gameDesignerBtn.*مصمم الألعاب الإلكترونية/s);assert.match(css,/\.designer-feature-btn span\{color:#17302f!important;opacity:1!important;font-weight:900!important\}/);});
test('v10 electronic game designer supports levels five game types roles preview save and paper print',()=>{const js=fs.readFileSync('./game-designer-v10.js','utf8');for(const x of ["value=\"choice\"","value=\"mark\"","value=\"match\"","value=\"sort\"","value=\"order\"","gdLevel","correct","pair","group","order","function preview","function saveGame","function printGame","app360-electronic-game-v10"])assert.match(js,new RegExp(x));});
test('v10 game designer shares the large visual library and supports upload drag resize and legacy touch',()=>{const js=fs.readFileSync('./game-designer-v10.js','utf8');assert.match(js,/ScreenMoveDesignerAssets/);assert.match(js,/gdUpload/);assert.match(js,/ondragstart/);assert.match(js,/ondrop=drop/);assert.match(js,/gd-resize/);assert.match(js,/ontouchstart/);assert.match(js,/ensureRatio/);assert.doesNotMatch(js,/=>|\bconst\b|\blet\b|`/);});
test('v10 electronic game can print a paper version without exposing answer roles',()=>{const js=fs.readFileSync('./game-designer-v10.js','utf8');assert.match(js,/ورقة اللعبة مشتقة من التصميم الإلكتروني دون إظهار الحلول/);assert.match(js,/gd-print-board/);assert.match(js,/d\.type==='mark'/);});
test('service worker caches both new designer runtimes',()=>{const sw=fs.readFileSync('./sw.js','utf8');assert.match(sw,/designer-v10\.js\?v=1[012345]/);assert.match(sw,/game-designer-v10\.js\?v=1[012345]/);assert.match(sw,/v10\.css\?v=1[012345]/);});

test('v11 worksheet and electronic game settings open by default and remain user controlled',()=>{const w=fs.readFileSync('./designer-v10.js','utf8');const g=fs.readFileSync('./game-designer-v10.js','utf8');assert.match(w,/toggle\.title=.*إظهار أو إخفاء/);assert.match(w,/open\(\);/);assert.doesNotMatch(w,/page\.addEventListener\('mousedown',close/);assert.match(g,/ensureRatio\(el\('gdBoard'\),W,H\);openSettings\(\)/);assert.doesNotMatch(g,/board\.ontouchstart=function\(\)\{closeSettings/);});
test('v11 library places create-your-own designer invitation after the game grid with both designer buttons',()=>{const js=fs.readFileSync('./app.js','utf8');const iGrid=js.indexOf("h+='</div><section class=\"library-designer-feature library-designer-footer");assert.ok(iGrid>0);assert.match(js,/لم تجد اللعبة التي تريدها؟/);assert.match(js,/libraryGameDesigner/);assert.match(js,/libraryDesigner/);});
test('v11 designer invitation title has explicit non-white high-contrast color',()=>{const css=fs.readFileSync('./v10.css','utf8');assert.match(css,/library-designer-footer h3\{color:#17302f!important/);assert.match(css,/Android 4\.4 \/ older WebKit fallbacks/);assert.match(css,/display:-webkit-flex;display:flex/);});
test('v13 cache version is active',()=>{const sw=fs.readFileSync('./sw.js','utf8');const html=fs.readFileSync('./index.html','utf8');assert.match(sw,/screen-to-move-v15/);assert.match(html,/v13\.css\?v=15/);assert.match(html,/legacy-v13\.js\?v=15/);});


test('v12 adds exactly twenty new games to every readiness level',()=>{const extra=require('./games-data-v12.js');assert.equal(extra.length,80);for(let level=1;level<=4;level++)assert.equal(extra.filter(g=>g.level===level).length,20);assert.equal(extra[0].id,'G91');assert.equal(extra[79].id,'G170');});
test('v12 new games have unique titles supported play schemas and five-step guidance',()=>{const extra=require('./games-data-v12.js');const supported=new Set(['choice','match','sort','dots','path','pattern','memory','order','puzzle','mark']);assert.equal(new Set(extra.map(g=>g.title)).size,80);for(const g of extra){assert.ok(supported.has(g.type),g.id);assert.equal(g.frames.length,5,g.id);assert.ok(g.materials.length>=1,g.id);if(g.type==='choice'||g.type==='pattern'){assert.ok(Array.isArray(g.options)&&g.options.length>=3,g.id);assert.ok(Number.isInteger(g.answer),g.id)}if(g.type==='match')assert.ok(g.pairs.length>=3,g.id);if(g.type==='sort')assert.ok(g.bins.length>=2&&g.items.length>=4,g.id);if(g.type==='memory')assert.ok(g.cards.length>=3,g.id);if(g.type==='order')assert.ok(g.order.length>=4,g.id);if(g.type==='mark')assert.ok(g.markOptions.length>=12,g.id);}});
test('v12 progression increases memory and visual planning demand across levels',()=>{const extra=require('./games-data-v12.js');const mem=l=>extra.filter(g=>g.level===l&&g.type==='memory').reduce((a,g)=>a+g.cards.length,0);const dots=l=>extra.filter(g=>g.level===l&&g.type==='dots').reduce((a,g)=>a+g.points.length,0);assert.ok(mem(4)>mem(1));assert.ok(dots(4)>dots(1));});
test('v12 creates a dedicated SVG cover for every new game',()=>{const extra=require('./games-data-v12.js');for(const g of extra)assert.ok(fs.existsSync('./assets/game-covers/'+g.id+'.svg'),g.id);});
test('v12 loads extra games before app runtime and adds covers to both designers',()=>{const html=fs.readFileSync('./index.html','utf8');assert.ok(html.indexOf('games-data-v12.js?v=15')<html.indexOf('app.js?v=15'));assert.ok(html.indexOf('designer-assets-v12.js?v=15')<html.indexOf('designer-v9.js?v=15'));const assets=fs.readFileSync('./designer-assets-v12.js','utf8');assert.match(assets,/G91\.svg/);assert.match(assets,/G170\.svg/);});
test('v12 extra game runtime stays ES5 friendly for Android 4.4 class browsers',()=>{const js=fs.readFileSync('./games-data-v12.js','utf8');assert.doesNotMatch(js,/=>|\bconst\b|\blet\b|\x60/);const app=fs.readFileSync('./app.js','utf8');assert.match(app,/EXTRA_VISUAL_NAMES_V12/);});
test('v12 service worker caches extra game data designer catalog and final cover',()=>{const sw=fs.readFileSync('./sw.js','utf8');assert.match(sw,/games-data-v12\.js\?v=15/);assert.match(sw,/designer-assets-v12\.js\?v=15/);assert.match(sw,/game-covers\/G170\.svg/);});


test('v13 gives every overlay explicit legacy-safe viewport edges',()=>{const css=fs.readFileSync('./v13.css','utf8');assert.match(css,/\.sheet,\.drawing-overlay\{[^}]*top:0!important;right:0!important;bottom:0!important;left:0!important/s);assert.match(css,/legacy-android \.sheet-card/);assert.match(css,/legacy-android \.drawing-panel/);});
test('v13 legacy runtime forces visible overlays into the old Android viewport',()=>{const js=fs.readFileSync('./legacy-v13.js','utf8');assert.match(js,/Android\\s/);assert.match(js,/function forceOverlay/);assert.match(js,/drawingOverlay/);assert.match(js,/sheet/);assert.doesNotMatch(js,/=>|\bconst\b|\blet\b|`/);});
test('v13 uses real teaching images for missing legacy glyphs and the shoe',()=>{const js=fs.readFileSync('./legacy-v13.js','utf8');for(const x of ['w0028.jpg','w0029.jpg','w0030.jpg','w0392.jpg','w0032.jpg','w0025.jpg','w0026.jpg'])assert.match(js,new RegExp(x.replace('.','\\.')));const shoe=fs.readFileSync('../say-and-name/assets/objects/shoe.svg','utf8');assert.match(shoe,/حذاء رياضي/);assert.match(shoe,/4f8bd6/);});
test('v13 compact landscape mode targets wide low-height tablets',()=>{const css=fs.readFileSync('./v13.css','utf8');assert.match(css,/@media \(min-width:800px\) and \(max-height:700px\)/);assert.match(css,/\.game-card\{width:calc\(20% - 8px\)/);assert.match(css,/\.challenge\{min-height:205px/);assert.match(css,/\.score-hud\{display:-webkit-flex/);});
test('v13 service worker precaches legacy runtime css and real clothing images',()=>{const sw=fs.readFileSync('./sw.js','utf8');assert.match(sw,/v13\.css\?v=15/);assert.match(sw,/legacy-v13\.js\?v=15/);assert.match(sw,/word-images\/w0028\.jpg/);assert.match(sw,/word-images\/w0392\.jpg/);});


test('v14 restores laptop layout while preserving legacy tablet compact mode',()=>{const css=fs.readFileSync('./v14.css','utf8');assert.match(css,/@media \(min-width:1200px\)/);assert.match(css,/html:not\(\.legacy-android\) \.game-card\{width:calc\(25% - 16px\)/);assert.match(css,/game-cover-art\{height:148px/);});
test('v14 puts designer settings opposite the close control',()=>{const css=fs.readFileSync('./v14.css','utf8');assert.match(css,/\.wd-settings-toggle,\.gd-settings-toggle\{left:auto!important;right:10px!important/);assert.match(css,/\.wd-settings-drawer,\.gd-settings\{left:auto!important;right:8px!important/);});
test('v14 adds legacy touch drag and drop for both designers',()=>{const js=fs.readFileSync('./legacy-v15.js','utf8');assert.match(js,/wd-asset/);assert.match(js,/gd-asset/);assert.match(js,/wd-item/);assert.match(js,/gd-item/);assert.match(js,/dispatchMouse/);assert.match(js,/elementFromPoint/);assert.doesNotMatch(js,/=>|\bconst\b|\blet\b/);});
test('v14 guards unfinished designer work from accidental close back update and reload',()=>{const js=fs.readFileSync('./legacy-v15.js','utf8');assert.match(js,/beforeunload/);assert.match(js,/popstate/);assert.match(js,/controllerchange/);assert.match(js,/drawing-backdrop/);assert.match(js,/updateBtn/);assert.match(js,/App360DesignerGuard/);});
test('v14 service worker waits for explicit activation instead of surprise reload',()=>{const sw=fs.readFileSync('./sw.js','utf8');assert.match(sw,/screen-to-move-v15/);assert.match(sw,/legacy-v15\.js\?v=15/);assert.match(sw,/v14\.css\?v=15/);const install=sw.match(/self\.addEventListener\('install',[^\n]+/)[0];assert.doesNotMatch(install,/skipWaiting/);assert.match(sw,/SKIP_WAITING/);});
test('v14 guard loads before app runtime so it can intercept back and service-worker reloads',()=>{const html=fs.readFileSync('./index.html','utf8');assert.ok(html.indexOf('legacy-v15.js?v=15')<html.indexOf('app.js?v=15'));assert.match(html,/v14\.css\?v=15/);});


test('v15 fast legacy manipulation avoids full rerender on every touch move',()=>{const wd=fs.readFileSync('./designer-v9.js','utf8'),gd=fs.readFileSync('./game-designer-v10.js','utf8');assert.match(wd,/paintItemFast/);assert.match(wd,/sx:d\.w\/Math\.max/);assert.match(gd,/paintGameItemFast/);assert.match(gd,/sx:W\/Math\.max/);});
test('v15 worksheet shapes can be dragged and placed at the requested point',()=>{const wd=fs.readFileSync('./designer-v9.js','utf8'),ui=fs.readFileSync('./designer-v15.js','utf8'),legacy=fs.readFileSync('./legacy-v15.js','utf8');assert.match(wd,/placeToolAtClient/);assert.match(wd,/text\/x-app360-tool/);assert.match(ui,/data-wdshape/);assert.match(legacy,/type:'tool'/);});
test('v15 large modern screens keep inspector on the right and workspace beside it',()=>{const css=fs.readFileSync('./v15.css','utf8');assert.match(css,/@media \(min-width:1000px\)/);assert.match(css,/wd-settings-drawer\{display:block!important/);assert.match(css,/wd-work\{margin-right:390px/);assert.match(css,/gd-work\{margin-right:390px/);});
test('v15 adds alignment distribution snap normalize and magic finish to both designers',()=>{const wd=fs.readFileSync('./designer-v9.js','utf8'),gd=fs.readFileSync('./game-designer-v10.js','utf8'),ui=fs.readFileSync('./designer-v15.js','utf8');for(const k of ['alignSelected','distributeAll','snapAll','normalizeSizes','smartFinish']){assert.match(wd,new RegExp(k));assert.match(gd,new RegExp(k))}assert.match(ui,/تحسين سحري للتصميم/);});
test('v15 uses IndexedDB on modern browsers with localStorage legacy fallback and autosaved drafts',()=>{const st=fs.readFileSync('./storage-v15.js','utf8'),ui=fs.readFileSync('./designer-v15.js','utf8');assert.match(st,/indexedDB/);assert.match(st,/LocalStorage/);assert.match(st,/saveDraft/);assert.match(st,/loadDraft/);assert.match(ui,/2500/);assert.match(ui,/استرجاع المسودة/);});
test('v15 assets are versioned and cached offline',()=>{const html=fs.readFileSync('./index.html','utf8'),sw=fs.readFileSync('./sw.js','utf8');assert.match(html,/v15\.css\?v=15/);assert.match(html,/storage-v15\.js\?v=15/);assert.match(html,/legacy-v15\.js\?v=15/);assert.match(html,/designer-v15\.js\?v=15/);assert.match(sw,/screen-to-move-v15/);assert.match(sw,/designer-v15\.js\?v=15/);});
