'use strict';
var crypto=require('crypto');
function now(){return new Date().toISOString()}
function hash(v){return crypto.createHash('sha256').update(String(v||'')).digest('hex')}
function uid(p){return(p||'id')+'-'+Date.now().toString(36)+'-'+crypto.randomBytes(5).toString('hex')}
function token(){return crypto.randomBytes(32).toString('hex')}
function safeJson(v){try{return JSON.stringify(v||{})}catch(e){return'{}'}}
function internalId(prefix,value){return prefix+'-'+hash(value).slice(0,24)}
async function ensureExternalUser(db,identity){
  identity=identity||{};var source='safe-reels-360',external=String(identity.external_user_id||''),link,userId,user,accountId=null;
  if(!external)throw new Error('external_user_id required');
  link=await db('external_identities').where({source:source,external_user_id:external}).first();
  if(link){userId=link.user_id;await db('external_identities').where({source:source,external_user_id:external}).update({sync_status:'linked',last_synced_at:now(),metadata_json:safeJson({external_role:identity.external_role,age_band:identity.age_band})})}
  else{
    userId=internalId('user-sr',external);user=await db('users').where({id:userId}).first();
    if(!user)await db('users').insert({id:userId,display_name:identity.display_name||'متدرب',role:identity.role||'learner',age_band:identity.age_band||null,status:'active',preferences_json:safeJson({identity_source:source})});
    await db('external_identities').insert({user_id:userId,source:source,external_user_id:external,sync_status:'linked',last_synced_at:now(),metadata_json:safeJson({external_role:identity.external_role,age_band:identity.age_band})});
  }
  await db('users').where({id:userId}).update({display_name:identity.display_name||'متدرب',role:identity.role||'learner',age_band:identity.age_band||null,status:'active',updated_at:now()});
  if(identity.account_external_id){
    accountId=internalId('acct-sr',identity.account_external_id);var acct=await db('accounts').where({id:accountId}).first();
    if(!acct)await db('accounts').insert({id:accountId,type:identity.role==='trainer'?'institution':'family',name:identity.account_name||'حساب الريلز الآمن 360',status:'active',metadata_json:safeJson({external_account_id:identity.account_external_id,source:source})});
    await db('account_members').insert({account_id:accountId,user_id:userId,relationship:identity.role==='trainer'?'trainer':'learner',permissions_json:safeJson(['practice'])}).onConflict(['account_id','user_id']).merge();
  }
  user=await db('users').where({id:userId}).first();return{user:user,account_id:accountId}
}
async function createIdentitySession(db,input){
  input=input||{};var raw=token(),ttl=Math.max(1,Math.min(Number(input.ttl_hours)||12,168)),exp=new Date(Date.now()+ttl*3600000).toISOString(),row={id:uid('auth'),user_id:input.user_id,account_id:input.account_id||null,source:input.source||'safe-reels-360',token_hash:hash(raw),external_session_ref:input.external_session_ref||null,status:'active',expires_at:exp,last_seen_at:now()};await db('identity_sessions').insert(row);return{token:raw,session:row}}
async function findIdentitySession(db,raw){if(!raw)return null;var row=await db('identity_sessions').where({token_hash:hash(raw),status:'active'}).first();if(!row)return null;if(new Date(row.expires_at).getTime()<=Date.now()){await db('identity_sessions').where({id:row.id}).update({status:'expired'});return null}await db('identity_sessions').where({id:row.id}).update({last_seen_at:now()});var user=await db('users').where({id:row.user_id,status:'active'}).first();if(!user)return null;return{session:row,user:user,account_id:row.account_id||null}}
async function revokeIdentitySession(db,raw){if(!raw)return false;var n=await db('identity_sessions').where({token_hash:hash(raw),status:'active'}).update({status:'revoked',last_seen_at:now()});return n>0}
function positive(n,max){n=Math.round(Number(n)||0);if(n<0)n=0;if(max&&n>max)n=max;return n}
async function importUsageBatch(db,userId,batch){
  batch=batch||{};var batchId=String(batch.batch_id||''),entries=Array.isArray(batch.entries)?batch.entries:[],saved=0,duplicates=0;if(!batchId)throw new Error('batch_id required');if(entries.length>100)throw new Error('usage batch too large');
  await db.transaction(async function(trx){for(var i=0;i<entries.length;i++){var e=entries[i]||{},appId=String(e.app_id||''),day=String(e.occurred_day||now().slice(0,10)).slice(0,10),seconds=positive(e.active_seconds,86400),opens=positive(e.opens,1000),receiptId=batchId+':'+appId+':'+day;if(!appId)continue;var app=await trx('applications').where({id:appId}).first();if(!app)continue;var oldReceipt=await trx('usage_receipts').where({id:receiptId}).first();if(oldReceipt){duplicates++;continue}await trx('usage_receipts').insert({id:receiptId,user_id:userId,app_id:appId,batch_id:batchId,occurred_day:day,active_seconds:seconds,opens:opens,device_id:batch.device_id||null});var row=await trx('app_usage_daily').where({user_id:userId,app_id:appId,day:day}).first();if(row)await trx('app_usage_daily').where({user_id:userId,app_id:appId,day:day}).update({active_seconds:(row.active_seconds||0)+seconds,opens:(row.opens||0)+opens,last_seen_at:now(),updated_at:now()});else await trx('app_usage_daily').insert({user_id:userId,app_id:appId,day:day,active_seconds:seconds,opens:opens,last_seen_at:now(),updated_at:now()});saved++}}});return{saved:saved,duplicates:duplicates,batch_id:batchId}}
async function usageDashboard(db,userId){var rows=await db('app_usage_daily as u').leftJoin('applications as a','a.id','u.app_id').where('u.user_id',userId).groupBy('u.app_id','a.title_ar').select('u.app_id','a.title_ar').sum({active_seconds:'u.active_seconds'}).sum({opens:'u.opens'}).max({last_seen_at:'u.last_seen_at'}).orderBy('active_seconds','desc');var total=0;for(var i=0;i<rows.length;i++){rows[i].active_seconds=Number(rows[i].active_seconds)||0;rows[i].opens=Number(rows[i].opens)||0;total+=rows[i].active_seconds}return{user_id:userId,total_active_seconds:total,apps:rows}}
async function upsertUserContentBatch(db,userId,batch){
  batch=batch||{};var items=Array.isArray(batch.items)?batch.items:[],saved=0;if(items.length>100)throw new Error('content batch too large');await db.transaction(async function(trx){for(var i=0;i<items.length;i++){var x=items[i]||{},appId=String(x.app_id||''),clientKey=String(x.client_key||''),type=String(x.content_type||'user_content'),title=String(x.title||'محتوى شخصي').slice(0,240);if(!appId||!clientKey)continue;var app=await trx('applications').where({id:appId}).first();if(!app)continue;var existing=await trx('content_items').where({owner_user_id:userId,app_id:appId,client_key:clientKey}).first(),content=safeJson(x.content||{}),id=existing?existing.id:internalId('uc',userId+'|'+appId+'|'+clientKey),rev=existing?(existing.current_revision||1)+1:1;if(existing)await trx('content_items').where({id:id}).update({content_type:type,title:title,status:'draft',source_kind:'user-created',current_revision:rev,content_json:content,tags_json:safeJson(x.tags||[]),visibility:'private',origin_device_id:batch.device_id||null,updated_at:now()});else await trx('content_items').insert({id:id,app_id:appId,content_type:type,owner_user_id:userId,title:title,status:'draft',source_kind:'user-created',current_revision:1,content_json:content,tags_json:safeJson(x.tags||[]),client_key:clientKey,visibility:'private',moderation_status:'not_submitted',origin_device_id:batch.device_id||null});await trx('content_revisions').insert({content_item_id:id,revision:rev,changed_by_user_id:userId,change_kind:existing?'user-sync-update':'user-sync-create',content_json:content,change_note:'private account sync'});saved++}}});return{saved:saved}}
async function listUserContent(db,userId,appId){var q=db('content_items').where({owner_user_id:userId});if(appId)q=q.andWhere({app_id:appId});var rows=await q.orderBy('updated_at','desc').limit(200);return rows.map(function(r){var x={};for(var k in r)x[k]=r[k];try{x.content=JSON.parse(r.content_json||'{}')}catch(e){x.content={}}delete x.content_json;return x})}
module.exports={hashToken:hash,ensureExternalUser:ensureExternalUser,createIdentitySession:createIdentitySession,findIdentitySession:findIdentitySession,revokeIdentitySession:revokeIdentitySession,importUsageBatch:importUsageBatch,usageDashboard:usageDashboard,upsertUserContentBatch:upsertUserContentBatch,listUserContent:listUserContent};
