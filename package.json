{
  "name": "app360-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "مختبر التطبيق 360 - practical learning applications monorepo for Safe Reels 360",
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node tooling/validate-platform.js",
    "check": "pnpm run validate",
    "workspaces:list": "pnpm -r list --depth -1",
    "clean:workspace": "node tooling/clean-workspace.js"
  }
}
